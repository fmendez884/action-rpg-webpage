import React from "react";
import ReactDom from "react-dom"
import { connect } from "react-redux";

export default class DummyWebPlayer extends React.Component {
    render() {
        return (
            <div id="DummyWebPlayer">
            </div>
        );
    }
}